# AGENTS.md — Правила игры и архитектура

## Правила игры

### Колода и правила раздачи
- Игра называется "Свои козыри"
- Игра ведётся обычной карточной колодой в 36 карт
- Играют 4 игрока
- Перед каждой сдачей карт игрокам колода тасуется
- Карты раздаются по очереди, по одной, каждому игроку, по часовой стрелке, начиная с игрока, следующего после дилера, раздаются все карты имеющиеся в игровой колоде
- Перед самой первой сдачей карт дилер выбирается случайно, при следущих раздачах дилером становится игрок проигравший на предыдущем раунде игры

### Ход игры
- Каждому из игроков назначается своя масть в качестве козырной
- Из изначальной колоды извлекаются все карты достоинстов "6" каждой масти и выкладываются отдельно, это будет нужно в дальнейшем для отслживания хода игры. Место, где лежат эти карты будем именовать "Табло"
- Раздаются карты, по правилам, определённым выше
- Если у кого-то из игроков есть чужая козырная карта наименьшего достоинства (для первого раунда это семёрка), то она передаются тому игроку, кому эта масть назначена в качестве козырной. Из этого правила есть одно исключение, если у игрока из его масти остался только туз, то туз не подлежит такой передаче в начале раунда.
- Первый ход совершает дилер. Он кладёт на игровой стол одну карту. Игрок, следующий ща ним по часовой стрелке, должен побить эту карту и положить сверху ещё одну. Побить карту можно картой той же масти, но большего достоинства или своим козырем.
- Если у игрока есть возможность побить карту, положенную перед ним на игровой стол, он обязан побить её, поддаваться не дорускается.
- Если игрок никак не может побить карту, то он забирает все карты, ранее выложенные на стол в этом раунде. Если остались ещё игроки с картами, то следующий игрок снова ходит с одной карты.
- Таким манером игровой раунд продолжается, пока не останется только один игрок с картами на руках. он является проигравшим в это раунде.
- Из игровой колоды извлекается карта наименьшего достоинства, той масти, которая назначена проигравшему раунд игроку в качестве козырной и кладётся на Табло поверх карт той же масти, что там уже лежат
- Таким образом проигравший игрок наказывается, уменьшением количества его козырей в колоде.
- Глядя на табло, другие игроки могут определять, сколько у оппонентов козырей и какую наименьшу карту чужой масти им отдавать в начале раунда.
- Когда игрок лишается всех своих козырей, он выбывает из игры. Игру продолжают оставшиеся игроки, пока не останется один победитель.

---

Техническое задание для кодинг-агента
---

## 1. Общая цель

Реализовать карточную игру как **чистое frontend-приложение**:

* TypeScript (strict mode)
* HTML5 Canvas (рендер)
* Без backend на первом этапе

При этом архитектура должна позволять:

* легко добавить backend в будущем
* перейти на multiplayer (WebSocket / server-authoritative)

---

## 2. Главный принцип

⚠️ КРИТИЧНО:

Несмотря на отсутствие backend, архитектура должна имитировать его наличие.

То есть:

* Game Logic ≠ UI
* State Manager ≈ "локальный сервер"
* UI — только клиент

---

## 3. Архитектурный стиль

Использовать:

* Clean Architecture (адаптированную под frontend)
* или Hexagonal (Ports & Adapters)

---

## 4. Структура проекта

```id="2t0p4f"
frontend/
 ├── domain/                # чистая логика игры
 │    ├── model/
 │    ├── rules/
 │    ├── services/
 │    └── events/
 │
 ├── application/           # use cases (как backend API)
 │    ├── usecases/
 │    ├── dto/
 │    ├── ports/
 │    │    ├── in/
 │    │    └── out/
 │
 ├── infrastructure/        # реализации (in-memory, storage)
 │    ├── persistence/
 │    ├── adapters/
 │    └── config/
 │
 ├── presentation/
 │    ├── canvas/
 │    ├── input/
 │    ├── ui/
 │    └── state/
 │
 └── main/
```

---

## 5. Domain слой (ядро)

### 5.1 Обязанности

* правила игры
* валидация ходов
* переходы состояний
* бизнес-логика

### 5.2 Ограничения

* ❌ никаких зависимостей от браузера
* ❌ никаких зависимостей от Canvas
* ❌ никаких side-effects
* ✔ только чистые функции и модели

---

### 5.3 Пример сущностей

* Game
* Player
* Card
* Deck
* Turn
* Action

---

### 5.4 Пример правила

```id="u0q8jo"
function canPlayCard(game: Game, action: PlayCardAction): boolean
```

---

## 6. Application слой (имитация backend)

### 6.1 Зачем он нужен

Даже без backend:

👉 этот слой = "локальный сервер"

---

### 6.2 Use Cases

Каждое действие — отдельный use case:

* createGame
* startGame
* playCard
* endTurn

---

### 6.3 Пример

```id="4c5zrl"
class PlayCardUseCase {
  execute(command: PlayCardCommand): GameStateDTO
}
```

---

### 6.4 Ports

#### In (вход)

* вызывается UI

#### Out (выход)

* GameRepository
* EventBus (для будущего realtime)

---

## 7. Infrastructure слой

### 7.1 In-memory storage

```id="f3e9qk"
class InMemoryGameRepository implements GameRepository
```

---

### 7.2 Local persistence (опционально)

* LocalStorage
* IndexedDB

---

### 7.3 Event Bus

* простая pub/sub система
* нужна для будущего WebSocket

---

## 8. Presentation слой

## 8.1 Принцип

UI НЕ содержит бизнес-логики

---

### 8.2 Подслои

#### State (важно)

* хранит текущий GameStateDTO
* синхронизируется через use cases

👉 это "client store", но НЕ источник истины

---

#### Canvas Renderer

* отрисовка:

  * карт
  * стола
  * игроков
* никаких правил

---

#### Input Layer

* mouse / touch
* переводит действия в команды

---

#### UI Layer

* кнопки
* панели
* HUD

---

## 9. Поток данных

```id="x6nh0m"
User Input
   ↓
Command
   ↓
UseCase
   ↓
Domain Logic
   ↓
New GameState
   ↓
State Store
   ↓
Renderer (Canvas)
```

---

## 10. Формат состояния

Использовать DTO:

```id="1my6kz"
type GameStateDTO = {
  gameId: string
  players: PlayerDTO[]
  table: CardDTO[]
  currentTurn: string
}
```

---

## 11. Подготовка к мультиплееру (ОЧЕНЬ ВАЖНО)

Даже без backend:

### 11.1 Обязательные правила

1. Все действия идут через Use Cases
2. Нет прямых мутаций state
3. GameState считается "пришедшим извне"

---

### 11.2 Лёгкий переход на сервер

В будущем заменить:

```id="q8r3r2"
PlayCardUseCase.execute(...)
```

на:

```id="zq1m0k"
POST /game/action
```

---

### 11.3 Event-ready архитектура

Добавить:

* GameUpdatedEvent
* TurnChangedEvent

---

## 12. Синхронизация состояния

### Сейчас:

* полный пересчёт состояния

### В будущем:

* diff / events

---

## 13. Тестирование

### Domain (обязательно)

* unit tests
* проверка правил

### Application

* тесты use cases

---

## 14. Требования к коду

### TypeScript

* "strict": true
* no any
* prefer readonly
* immutable state

---

## 15. MVP

1. Создание игры
2. Один игрок человек, ему назначена козырная масть пики
3. Остальные игроки боты, напиши для них простую логику
4. Отрисовка через Canvas
5. Обновление состояния
6. Табло располагается в левом верхнем углу, визуально отображается как 4 карты, лежащие в один ряд
7. Карты в руке игрока, сгруппированы по мастям и отсортированы внутри масти в порядке возрастания. Порядок мастей в руке игрока повторяет порядок мастей игроков за столом
8. Возле каждого игрока графически отображена назначенная ему масть


---

## 16. Запрещено

❌ писать логику игры в Canvas
❌ менять state напрямую из UI
❌ связывать Domain с браузером

---

## 17. Definition of Done

* игра полностью работает локально
* архитектура разделена на слои
* можно добавить backend без переписывания логики
* код легко масштабируется

